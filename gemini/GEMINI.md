# Pivotly

This extension adds the Pivotly MCP server and the commands below.

## Commands

- `/pivotly:hello-world` — Confirm the Pivotly plugin is installed and connected — greet, then report the plugin's details, the MCP server's connection status, and the tools it exposes.

Each command carries its own procedure and pitfalls; invoke one rather than guessing.
